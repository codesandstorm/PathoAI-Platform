# Ablation Studies Framework

The `AblationEngine` ([pathoai/validation/ablation.py](file:///d:/Research/PathoAI-Platform/pathoai/validation/ablation.py)) evaluates component contributions by systematically removing or varying individual pipeline modules (e.g. comparing performance with vs without Tumor Bulk extraction).
